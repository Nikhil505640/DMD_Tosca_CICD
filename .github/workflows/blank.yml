name: DMD_Tosca_Automation
 
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:
 
jobs:
  build:
    runs-on: [self-hosted, windows]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
 
      - name: Run TOSCA Test
        run: >
          C:\Tosca_CICD\ToscaCI\Client\ToscaCIClient.exe
          -m distributed
          -c "C:\Tosca_CICD\ToscaCI\Client\DMD.xml"
          -r "C:\Tosca_CICD\Automation\Result.xml"
 
      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Unit Test Results
          path: C:\Tosca_Projects\Automation\Result.xml
